# MoAI Desktop 배포 로그

Windows 데스크톱(Avalonia, 단일 exe) 릴리스 이력. CLI(`moai`)와 독립 버전.
배포본: `~/shareHub/Zone/MoAiDesktop_<버전>.exe` (self-contained, win-x64).

| 버전 | 요약 |
|------|------|
| 0.4.61 | **COM 편집 확장 4종 + 엑셀 양식 테두리**: 열린 문서 편집에 흔한 누락 기능 보강. (1) **찾기·바꾸기**(Word·Excel·PPT) — find_text→replace_text 전체 치환. (2) **PDF 내보내기**(3앱) — 열린 문서를 레이아웃 그대로 PDF(ExportAsFixedFormat), 경로 지정 또는 원본 옆 파생. (3) **삭제 세트** — PPT 슬라이드 / Excel 시트(마지막 시트 가드)·행·열 / 3앱 도형 삭제. (4) **Excel 셀 병합**(가운데 정렬, 양식 제목·결재란용). + **엑셀 업무 양식에 표 테두리** — 인쇄/PDF에서도 격자가 보여 실제 양식처럼 렌더(공용 라이터 무침습, 일반 시트는 기존 동작 유지) |
| 0.4.60 | **문서·엑셀 양식 템플릿**: 표준 업무 서식을 규격화(구조는 앱이 보장, 내용만 채움). (1) **Word** DocxCreate에 **template** — report(보고서: 개요·배경·현황·분석·결론 및 제언)/incident(경위서: 발생 개요·경위·원인·조치·재발 방지)/proposal(제안서: 배경·제안 내용·기대 효과·추진 일정·소요 예산). (2) **Excel** XlsxCreate에 **template** — expense(지출결의서: 일자/적요/금액/비고 + 합계 SUM·원화)/invoice(거래명세서: 품목/규격/수량/단가/금액 + 합계)/inventory(재고관리표: 품목/규격/입고/출고/재고). template만 주면 빈 양식, sheets를 함께 주면 값 채움 |
| 0.4.59 | **PPT 디자인 템플릿 4종**: A(연그레이·남색 코퍼레이트) / B(흰·빨강 키노트) / C(미니멀·검정) / D(다크·시안). template 로 선택, 디자인은 규격이 보장 |
| 0.4.58 | **PPT 템플릿 시스템(디자인 프리셋 × 레이아웃)**: "이쁘게 만들어" 대신 검증된 템플릿 규격 안에서 콘텐츠만 채운다. PptxCreate에 **template**(A 코퍼레이트 / B 키노트 — 배경·색·타이포 계층·정렬 고정) × **layout**(cover·section·content·two_col·text_image·table·quote) + **subtitle**. 타이포 계층·좌측 정렬·줄간격으로 매달린 줄·과밀 해소. + 3앱 단위 추가 통일(**add_slide**·**add_page**·add_sheet)로 빈 문서 초안 생성 |
| 0.4.57 | **엑셀 피봇테이블 생성**: Excel 편집에 **insert_pivot** 추가 — 열린 워크북의 데이터 범위(source)로 새 시트에 피봇테이블을 만든다. rows/columns/filters(필드명)와 values({field, func}: sum·count·average·max·min)를 지정하면 Excel 엔진이 집계(예: 부서×월 매출 합계). 없는 필드는 건너뛰고 부분 성공. Windows 전용 |
| 0.4.56 | **도형 편집(위치·크기·회전·반전) 3앱 통일 + 이미지 삽입**: (1) Word·Excel·PowerPoint 편집에 **set_geometry** 추가 — 도형 이동/크기/**회전(도)**/좌우·상하 반전(공용 ShapeGeometry). Word·Excel Inspect도 도형 목록(위치·크기·회전) 노출, PPT Inspect에 rotation 추가. (2) **이미지 삽입** — 신규 **ImageFetch**(웹 이미지 URL→로컬 저장, SSRF 가드·image/* 검증)로 웹 이미지 확보, 세 앱 편집에 **insert_picture**(열린 문서에 로컬 이미지 삽입: PPT=슬라이드, Word=인라인/떠있는 도형, Excel=활성시트). 생성 이미지(ImageCreate)·웹 이미지 모두 삽입 가능 |
| 0.4.55 | 스킬 개별 토글 일관성: CLI에서 끈 개별 스킬(skills-disabled.json)을 데스크톱에서도 제외(마스터 스위치와 별개로 존중) |
| 0.4.54 | **스킬 지원 + 설정 on/off**: 데스크톱 에이전트가 사용자·팀 공유 스킬을 사용(SkillTool 배선). **설정 화면에 스킬 마스터 스위치** — 끄면 스킬 기능 비활성화. (팀 스킬은 로그인 시 받아둔 것을 로드; 자동 사용은 Windows 로그인 검증) |
| 0.4.53 | **파일 없음(orphaned) 감지**: 연결 문서가 이동/삭제되면 재방문 카드에 **"⚠ 파일을 찾을 수 없음"** 표시, 그 대화를 열면 "대화는 보존, 문서 다시 열면 이어서 편집" 안내(대화는 안 지움). 세션에 문서 경로 영속화 |
| 0.4.52 | **문서-대화 1:1 재연결**: 문서를 다시 열면 그 문서(MoaiDocId 정확 일치)에 연결된 **이전 대화를 자동으로 이어감**. 세션에 문서 식별자(DocId) 영속화, 열린 문서의 전체 경로(FullName)를 COM 으로 확보. (경로/유사도 기반 매칭·확인배너·파일없음은 후속) |
| 0.4.51 | **생성→편집 브릿지**: 프롬프트로 만든 문서 카드에 **[열어서 편집]** 추가 — Office 로 열어 같은 대화를 편집 세션으로 전환(생성·편집이 하나의 대화). 생성 문서엔 **MoAI 고유 식별자(MoaiDocId)** 를 심어 나중에 같은 대화로 되찾을 기반 마련. 홈 타일 아이콘 축소·가로 배치 |
| 0.4.50 | **홈 화면 상태화 + 대화 삭제**(UX 목업 반영): 첫 실행엔 예시 칩(이 견적서 요약해줘 등), 재방문엔 **최근 작업 카드**를 최상단에 노출. 대화 기록/홈 카드에 **삭제(✕)** 추가. 색은 기존 토큰이라 다크·라이트 자동 지원 |
| 0.4.49 | **"톤앤매너 맞춰"를 실제 문서에서 읽음**: PowerPointInspect가 도형 **채우기 색(fill_color)** + 문서 **지배 색상 팔레트(dominant_colors)** 를 노출. 기존엔 폰트 색만 실측하고 도형 색은 대화 맥락(프롬프트 기억)에 의존 → 새 대화방/기존 문서에서도 실제 팔레트 기준으로 톤 매칭 |
| 0.4.48 | **PPT 편집이 슬라이드 마스터/레이아웃까지 도달**: 전체 색상 톤 변경 시 마스터/레이아웃의 장식 도형(배너·사이드바)이 안 바뀌던 문제. PowerPointInspect가 각 도형의 **scope(slide/layout/master)** 를 노출, PowerPointEdit에 **scope 지정** 추가 — 마스터/레이아웃 도형도 편집. (마스터 변경은 공유 슬라이드 전체에 반영되므로 미리보기 게이트로 확인) |
| 0.4.47 | 확인 카드(미리보기 게이트) 문구가 길면 상자 밖으로 넘치던 문제 수정 — 아이콘+텍스트를 **Grid(Auto,*)** 로 제약해 줄바꿈(가로 StackPanel 이 무한 너비를 줘 wrap 이 안 되던 원인) |
| 0.4.46 | **문서 생성 표현력 확장(Excel·PPT)**: (1) **Excel 셀 자동 타이핑** — "12.5%"·"1,234"·"=SUM(...)"을 실제 퍼센트·천단위·라이브 수식으로. (2) **열 단위 서식**(won/usd/percent/thousands/date 또는 원시 코드) — 수식 결과에도 서식 적용, **자동 열 너비**로 '###' 잘림 방지. (3) **PPT auto-fit** — 불릿 개수·표 행수에 따라 폰트·행높이를 줄여 슬라이드 밖으로 넘치지 않게 |
| 0.4.45 | **문서 입력(참조) 품질 개선**: (1) **Read가 Office/PDF를 내장 추출기로 직접 읽음**(외부 도구·스크립트 불필요, 단일 바이너리 유지) — 모델이 docx/xlsx/pptx/pdf를 그냥 Read해서 참조 가능. (2) **구조 보존 추출** — docx 제목/목록/표를 마크다운(#·-·표)으로, xlsx 시트명·pptx 슬라이드 경계 표시. Read·청킹·조직검색·첨부가 같은 추출기를 공유해 전 경로 동시 개선 |
| 0.4.44 | **문서 생성 품질 개선**: (1) 문서 툴 세션 전용 시스템 프롬프트 지침 추가 — 코딩용 "간결하게" 규칙이 문서 본문과 상충하던 문제 해소, 문서유형별 구조·톤·편집 시 서식매칭 지시. (2) **DocxCreate 대폭 확장** — 제목레벨/글머리표·번호목록/표/본문 **굵게**·*기울임* 인라인 서식(기존 flat paragraphs 하위호환). LibreOffice 렌더 검증 |
| 0.4.43 | **답변 가림 최종 해결**: 하단 여백을 `ScrollViewer.Padding`(=Extent 미포함, sticky의 Extent-Viewport 계산에서 무시됨)에서 **스크롤 콘텐츠 Margin(56)** 으로 이동. 이제 마지막 말풍선이 입력창 위로 여백을 두고 온전히 표시. + 중간 Panel 래퍼 제거(Grid star-row 높이 제약 직접 전달) |
| 0.4.42 | **답변 가림 근본 해결**: 입력창을 앱 Grid 최상위 **하단 Row(Auto)로 물리 분리**, 채팅 ScrollViewer는 Row0(*)에서 constrained. 스크롤바가 더 이상 입력창을 침범하지 않음(스크롤 정상) |
| 0.4.41 | 스크롤 정석 재구현(웹리서치 기반): **StickyBottomScroll**(ScrollChanged로 Extent 변경마다 하단 재핀, 사용자 위로 올리면 해제) + **AllowAutoHide=False**(오버레이 스크롤바 겹침 방지). 임기응변 스크롤 로직 제거 |
| 0.4.40 | **답변 가림 근본 수정**: 채팅 영역을 Grid(RowDefinitions) → **DockPanel(입력=Dock Bottom, 스크롤=Fill)** 로 재구성. Grid 가 스크롤 영역을 입력창까지 확장(겹침)시키던 문제. + 하단 여백 90 유지 |
| 0.4.39 | 답변 가림 물리적 해결: 채팅 **하단 Padding 90**(입력창보다 크게)로 마지막 메시지 항상 위로 밀기 + near-bottom 추적을 다음 프레임+Offset 강제로 보정 |
| 0.4.38 | **답변 가림 근본 수정**: MarkdownBlock(스트리밍 답변)에 near-bottom 자동 추적 — 높이가 커질 때 하단 근처면 따라 스크롤, 위로 읽는 중엔 방해 안 함 |
| 0.4.37 | 대화 기록·열린 문서 항목이 2줄로 wrap되던 것 → **NoWrap+말줄임(…)** 1줄로(전역 TextWrapping=Wrap 오버라이드) |
| 0.4.36 | 대화 기록·열린 문서 항목을 **Grid(Auto,*)** 로 바꿔 제목 …말줄임 정상화(스크롤바 침범 방지), 채팅 스크롤을 **Offset 직접 설정+6프레임**으로 강화(답변/카드 가림) |
| 0.4.35 | 좌패널 리스트(대화 기록·열린 문서)에 우측 여백 추가 — 오버레이 스크롤바가 항목 텍스트를 가리던 문제 |
| 0.4.34 | Word 편집 실패 수정: 문단 `set_text` 시 **¶(문단기호) 제외**('줄 끝 작업 잘못' 0x800A1483 해결), 문단 범위 초과 에러에 **insert_paragraph 안내**, Description에 "존재 문단만 편집" 강조 |
| 0.4.33 | 전송 버튼 색을 **입력창 윤곽선과 동일**하게(편집 연결 중 앱 색, 미연결 시 기본 accent) |
| 0.4.32 | 답변 마지막이 입력창에 가리는 문제: **스트리밍 완료 후 최종 스크롤** + LayoutUpdated를 **여러 프레임 유지**(스트리밍 중 늘어나는 높이 추적) |
| 0.4.31 | 미리보기 확인 카드를 **결정 후 제거**(작업 후에도 큰 상자로 남던 문제) |
| 0.4.30 | 채팅 메시지 영역과 입력창을 **동일한 star 3열(중앙 3*, MaxWidth 760)** 로 통일 — 폭·좌우 정렬이 어긋나던 문제 수정 |
| 0.4.29 | **Inspect가 문서 서식 노출**(문단/도형별 크기·글꼴·굵기·색) → 모델이 문서 톤을 보고 편집 판단. Inspect 설명에 "기존 톤에 맞추라" 지시 |
| 0.4.28 | Word `insert_paragraph` 기본 **본문(Normal) 스타일**(앞 제목 48pt 상속 방지), PowerPoint `set_text` 서식 보존을 크기+**굵기·글꼴명**까지 확장 |
| 0.4.27 | 채팅 자동 스크롤을 **LayoutUpdated(실제 레이아웃 완료 시점)** 에 실행하도록 변경 — 방금 추가된 마지막 카드가 배치되기 전 스크롤돼 끝까지 못 가던 문제(입력창 가림) 수정 |
| 0.4.26 | 미리보기 게이트에 **"계속 허용"** 추가(병렬·연속 편집 확인 폭주 방지 — 한 번 누르면 세션 동안 자동 승인), 채팅 자동 스크롤을 **2단 우선순위**로 개선(끝까지 확실히) |
| 0.4.25 | **Word/Excel COM 편집 도구 신설**(PowerPoint에 이어 풀 편집). Word: 조회 + 텍스트·글꼴·스타일·문단 삽입/삭제·표. Excel: 값·수식·글꼴·채우기·차트·시트. 미리보기 게이트·진행 문구·색 헬퍼 공용화 |
| 0.4.24 | 진행 인디케이터("작업 중…" 바운스)를 **항상 맨 아래 유지** — 도구 사이·최종 답변 전 빈 구간에도 멈춰 보이지 않게(새 항목은 인디케이터 위에 삽입, 완료 시 제거) |
| 0.4.23 | 채팅 영역을 **Grid 2행**으로 재구성(스크롤 끝 겹침 수정), 연결 해제 시 **Flyout 명시적 닫기**(빈 값 재표시 버그), **열린 문서 클릭 시 오피스 창 앞으로**(app.Activate) |
| 0.4.22 | "편집 중" 칩 제거 → **입력창에 통합**(윤곽선이 연결 앱 색, 좌측 앱 버튼으로 해제 확인 팝업), 영역 선택 콤보 제거 · **set_text 폰트 크기 보존**(AutoFit로 3pt 축소되던 버그 수정) |
| 0.4.21 | 추천 질문 **행간 축소**(ContentPresenter 여백 상속 차단), 열린 문서·편집 칩을 **앱별 색**(PPT 빨강·Excel 녹색·Word 파랑)으로, 하단 입력영역 **대칭 star 컬럼**으로 정확한 중앙 정렬(0.4.20 좌측 편향 수정) |
| 0.4.20 | 편집 칩 [X] 닫을 때 입력창 폭 쪼그라듦 수정 |
| 0.4.19 | 아이콘 크기 축소, **Pencil·Presentation geometry 버그 수정**(상대 서브패스가 튀던 문제), 채팅 하단 여백 확대(가림 방지), 추천 질문 **1열 세로 나열**, 401 인증오류 시 **재로그인 안내** |
| 0.4.18 | **이모지 완전 제거 → lucide 통일**: 대화기록 배지·문서 카드·참조 칩·확인 카드·홈 타일·오피스 런처의 이모지를 모두 벡터 아이콘으로(키→geometry 컨버터), 본문 텍스트 이모지도 제거 |
| 0.4.17 | **아이콘 lucide 통일**(open-moai와 동일): 첨부·문서함·전송·설정·새로고침·새작업·계정·편집 칩을 2px 라인 아이콘으로 교체 |
| 0.4.16 | 편집 세션 진입 시 **추천 질문 버튼**, 활성 대상 칩·입력창 **채팅 본문과 중앙 정렬**, 답변 전 바운스 인디케이터 확인 |
| 0.4.15 | **좌패널 UX 통합안**(리서치 기반): 활성 대상 칩(대상·적용범위·해제), 열린 문서=런처, 대화 기록 유형 배지+파일명, 세션 유형/대상 저장 · **미리보기 게이트**(라이브 편집 전 적용/취소) |
| 0.4.13 | 도구 진행 배지 **구체 문구**(슬라이드 읽는 중/편집 중 등) |
| 0.4.12 | 진행 배지 **FIFO 매칭**(잔존 버그 수정) + **바운스 애니메이션** |
| 0.4.11 | PowerPoint 서식 편집 확장: **set_fill/set_font/set_line** + 현재 선택 도형 대상 |
| 0.4.10 | COM `dynamic` 캐스팅 크래시 수정 (**MsoTriState는 bool 아님, 좌표는 Single**) |
| 0.4.9 | COM 편집 실패 **상세 진단 로그**(HRESULT·apartment·예외 스택) |
| 0.4.8 | 오피스 런처에서 **열린 문서 선택 → 편집 세션 시작**(COM 활성화+채팅) |
| 0.4.7 | 홈 화면 재설계(새 대화/오피스 문서 작업 타일) · **Markdown.Avalonia 제거 → 네이티브 MarkdownBlock**(Avalonia 11.2 StaticBinding 크래시 해결) |
| 0.4.6 | 레벨별 파일 로거 `MoaiLog`(~/.moai/logs/moai.log, settings.json `logLevel`) + 전역 미처리 예외 훅 |
| 0.4.5 | SPA 재설계 최초(인-윈도우 로그인/설정, 좌패널 Office/기록 분할, MoAI 마크다운 말풍선) |
| ~0.4.4 | 초기 데스크톱(로그인·프록시·모델 선택·이미지 생성·문서 삽입·트레이 상주) |

## 로그·진단
- 파일 로그: `~/.moai/logs/moai.log` (레벨 `~/.moai/settings.json`의 `logLevel`, 기본 info; 상세 진단은 `"logLevel":"debug"`)
- 로그 메시지는 영어(ASCII) 전용 — CP949 등에서 깨짐 방지

## 알려진 제약
- COM Office 편집은 **Windows 전용**(리눅스 빌드/실행은 되지만 COM 목록은 비어 있음)
- 미리보기 게이트는 "무엇을 바꿀지" 요약 확인 방식 — 시각적 diff/undo는 미구현(검토 중)
